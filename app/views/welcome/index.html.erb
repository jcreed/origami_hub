<!--%= form_tag("/search", :method => "get") do %>
	<%= label_tag(:q, "Search_for:") %>
	<%= text_field_tag(:q) %>
	<%= submit_tag("Search") %>
	<div>
		<p>
		<%= check_box_tag(:pet_dog) %> 		
		<%= label_tag(:pet_dog, "I own a dog") %>
		<%= check_box_tag(:pet_cat) %>
		<%= label_tag(:pet_cat, "I own a cat") %>
	</p>
	</div>
	<div-->
<%= form_for( @origami, :url => origamis_path(@article), :html => {:method => "put"})  do |f| %>
	<!--%= options_for_select([['Bananas',1],['Oranges', 2],['Grapes',3],['Apples',4],[Kiwis,5]], 2) %-->
	<!--%= select_tag(:fruit_tag, options_for_select([['Bananas',1],['Oranges', 2],['Grapes',3],['Apples',4],['Kiwis',5]], 2)) %-->
	<%= collection_select(:order, :order_id, orig_name_desc, :id, :name) %>
	
<% end %>

